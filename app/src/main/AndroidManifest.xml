<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewGuesser"
        tools:targetApi="24">
        <activity
            android:name=".AddQuestions"
            android:exported="false" />
        <activity
            android:name=".ScoreBoard"
            android:exported="false" />
        <activity
            android:name=".TimedGameMode"
            android:exported="false" />
        <activity
            android:name=".CreateUser"
            android:exported="false" />
        <activity
            android:name=".BasicGameMode"
            android:exported="false" />
        <activity
            android:name=".OptionSelect"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>